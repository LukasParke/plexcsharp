# HasVoiceActivity

Voice activity detection availability flag returned by PMS.
PMS may return this as a boolean or as string values (`"0"` or `"1"`).



## Supported Types

### Boolean

```csharp
HasVoiceActivity.CreateBoolean(/* values here */);
```

### Two

```csharp
HasVoiceActivity.CreateTwo(/* values here */);
```
